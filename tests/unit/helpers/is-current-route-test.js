
import { isCurrentRoute } from 'fyp-app/helpers/is-current-route';
import { module, test } from 'qunit';

module('Unit | Helper | is current route');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isCurrentRoute([42]);
  assert.ok(result);
});

