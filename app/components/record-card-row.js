/*
@component record-card-row
@description
record card row for list record
@usage
{{record-card-row rowData=record index=index page=page perPage=perPage}}
*/
import Ember from 'ember';

export default Ember.Component.extend({
  tagName: ['div'],
});
